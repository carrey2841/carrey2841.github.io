import { Grid } from '@mui/material'
import { styled } from '@mui/material/styles'

export const NotFoundContainer = styled(Grid)({
  height: '100%',
  width: '100%',
})

export const NotFoundImageContainer = styled('img')({
  width: '110px',
})
